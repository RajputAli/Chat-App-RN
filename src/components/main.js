import { View, Text } from "react-native";
import BottomNavigator from "./Bottom";

export default function Main(){
    return(
        <View style={{flex: 1}}>
         <BottomNavigator/>
        </View>
    )
}